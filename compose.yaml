services:
  db:
    image: postgres:${POSTGRES_DOCKER_TAG}
    restart: unless-stopped
    env_file: stack.env
    volumes:
      - ./data/database:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT}:5432"
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: agro-tracking:local
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: stack.env
    environment:
      DB_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    volumes:
      - ./data/videos:/app/data/videos
      - ./data/failed_videos:/app/data/failed_videos
      - ./data/queue:/app/data/queue
    ports:
      - "8000:8000"
    command: ["uv", "run", "uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
    healthcheck:
      test:
        - CMD-SHELL
        - python -c "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  ingestion:
    image: agro-tracking:local
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    env_file: stack.env
    volumes:
      - ./data/queue:/app/data/queue
      - ./data/videos:/app/data/videos
      - ./data/failed_videos:/app/data/failed_videos
    command: ["uv", "run", "python", "-m", "ingestion.main"]
